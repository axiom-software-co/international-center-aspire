{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  
  // Migration Strategy Configuration (Microsoft recommended patterns)
  // RuntimeMigrations: Apply migrations at runtime (Development/Staging)
  // ProductionValidation: Validate pre-applied migrations (Production)
  // If not specified, auto-detects based on environment
  "MigrationStrategy": "RuntimeMigrations"
}
